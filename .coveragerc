[run]
omit =
    # omit everything in venv dir anywhere
    */venv/*
    */virtualenv/*
    # omit everything in test dir
    test/*

[report]
# Regexes for lines to exclude from consideration
exclude_lines =
    # Have to re-enable the standard pragma
    pragma: no cover